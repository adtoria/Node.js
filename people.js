const people = ['yoshi', 'ryu', 'chun-li', 'mario'];
const ages = [20, 25, 30, 35];

console.log(people);

// setting the export value
// exporting multiple values
module.exports = {
    people, ages
};